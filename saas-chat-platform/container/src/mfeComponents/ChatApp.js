import React from 'react';
import { Box, Typography } from '@mui/material';

// Este componente será substituído pelo MFE real quando estiver pronto
const ChatApp = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4">Chat</Typography>
      <Typography variant="body1">
        Este é um placeholder para o MFE de chat, que é o core da aplicação.
      </Typography>
    </Box>
  );
};

export default ChatApp;